/*
T[h(x)]に，LinkedListの先頭を持たせる．
Nodeにはvalueだけでなくkeyも持たせる．
そのため，<T> → <K, V>になる．
*/

/*
extern crate coding_interview;
use coding_interview::linked_list::*;

struct my_hash_table<T> {
    table: Vec<LinkedList<T>>,
}
*/

fn main() {}